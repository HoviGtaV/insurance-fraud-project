# Legacy code note

This folder contains older non-final development history kept for documentation and reproducibility of the project evolution.

Important:
- These files are NOT the official final active pipeline.
- The official final pipeline is listed in the root file FINAL_PIPELINE_MAP.md.
- The current active submission path is the root v3 scripts + src/cleaning + src/labels + src/qa + submission/*.
